<div class="container">
    <form class="form-horizontal" method="POST" action="/Album/Subir" enctype="multipart/form-data">
    <div  class="form-group">
        <label class="control-label col-sm-2" for="">MODO</label>
        <div class="col-sm-8"> 
            <label class="radio-inline">
                <input type="radio" value="foto"  name="tipo" checked id="rbTipo">FOTO
            </label>
            <label class="radio-inline">
                <input type="radio" value="album" name="tipo" id="rbTipo">ALBUM
            </label>
        </div>	 
    </div>
    
    <input type="hidden" name="municipio" value="<%=municipio%>">
    <div  class="form-group">
        <label for="fotos" class="control-label col-sm-2">FOTOS</label>
        <div class="col-sm-8">  
                <input id="fotos" name="fotos" type="file" class="file"  multiple data-preview-file-type="any" data-show-upload="false" required>
                <div id="errorBlock" class="help-block"></div>
        </div>	
    </div>     
    <div  class="form-group">
            <label class="control-label col-sm-2" for="descripcion">DESCRIPCION</label>
            <div class="col-sm-8"> 
                <input type="text" class="form-control" name="descripcion" id="txtDescripcion" placeholder="Ingrese descripción." required>
            </div>	
    </div>   			  
    <div class="form-group">
        <label class="control-label col-sm-2"></label>
        <div class="col-sm-8">  
        <button id="btnGuardar" class="btn btn-success" type="submit">Guardar</button>
        </div>
    </div>
    </form>
</div>
<script>
    /*
$('#file-5').fileinput({
     uploadUrl: '/subir',
     //uploadAsync: true,
     maxFileCount: 5,
     allowedFileExtensions: ['jpg', 'png', 'gif']
})
*/
$(document).ready(function(){
    
    $('#divAlbum').hide();
    $('#divFoto').show();
     $('#fotos').removeAttr( "multiple")
    $('input[type=radio][name=tipo]').change(function() {
        if (this.value == 'foto') {
           //$('#divAlbum').hide();
           //$('#divFoto').show();
           //$('#rbTipo').val('foto')
            $('#fotos').removeAttr( "multiple")
        }
        else if (this.value == 'album') {
            $('#fotos').attr( "multiple",true)
           //$('#divFoto').hide();
           //$('#divAlbum').show();
           //$('#rbTipo').val('album')
           
        }
    });
    /*
    $('#btnGuardar').click(function(e){
        var formData=new FormData();
        formData.append('descripcion',$('#txtDescripcion').val());
        formData.append('municipio','<%=municipio%>');
       //Subir foto
       //Subir album

        e.preventDefault();  
       
        if ($('#rbTipo').val()=='foto'){
            url='/Foto/Subir'
            formData.append('foto',$("#foto")[0].files[0]);      
        } else{
            url='/Album/Subir'
            formData.append('fotos',$("#fotos").files);           
        }
        
        $.ajax({url:url,data: formData,processData: false,contentType: false,type: 'POST'})
        .done(function(data){
            if(data){
                alert('Foto cargada.');
            }
        })                     
    })

    $('button[type="submit"]').click(function(){
        $.each($("#fotos")[0].files,function(i,file){
            var formData=new FormData();
            formData.append("foto",file);
              $.ajax({
                url:"/subir",
                data: formData,
                processData: false,
                contentType: false,
                type: 'POST',
                success: function(data){    
                }
             });
        })
    })
    */
})
</script>    